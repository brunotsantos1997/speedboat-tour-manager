// src/ui/components/icons.ts
import { Anchor, Utensils, Beer, User, Circle, Package } from 'lucide-react';

export const availableIcons = {
  Anchor,
  Utensils,
  Beer,
  User,
  Circle,
  Package,
};

export type IconKey = keyof typeof availableIcons;
